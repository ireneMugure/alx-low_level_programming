#include "main.h"
/**
 * mul - Multiplies two integers
 * @a:The first number
 *@b: Second number to multiply to first
 *Return: answer of the two numbers
 */
int mul(int a, int b)
{

	return (a * b);
}
